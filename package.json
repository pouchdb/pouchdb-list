{
	"name": "pouchdb-list",
	"version": "1.0.0",
	"main": "index.json",
	"description": "A PouchDB plug-in that allows you to re-use your CouchDB list functions on the client side.",
	"repository": {
		"type": "bzr",
		"url": "http://bazaar.launchpad.net/~marten-de-vries/python-pouchdb/0.x"
	},
	"dependencies": {
		"extend": "^1.2.1",
		"couchdb-objects": "^0.1.0",
		"couchdb-render": "^0.1.0",
		"pouchdb-req-http-query": "^0.1.0",
		"promise-nodify": "^1.0.0"
	},
	"devDependencies": {
		"es3ify": "^0.1.3"
	},
	"scripts": {
		"build-js": "mkdir -p ../../dist && browserify index.js -s List -o ../../dist/pouchdb-list.js",
		"build": "npm run build-js; cd ../../dist; uglifyjs pouchdb-list.js -mc > pouchdb-list.min.js"
	},
	"browserify": {
		"transform": [
			"es3ify"
		]
	}
}
